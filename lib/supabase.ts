// Placeholder file
// This is a temporary file to prevent module not found errors during the transition from supabase to local data
// It can be removed once all imports from @/lib/supabase have been replaced

export const supabase = {
  // Empty placeholder to prevent errors
};
